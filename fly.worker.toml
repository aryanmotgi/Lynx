app = "lynx-worker"
primary_region = "sjc"

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"

[processes]
  worker = "pnpm --filter @lynx/worker start"

# Browser needs RAM. One session per machine, scale horizontally.
[[vm]]
  cpu_kind = "shared"
  cpus = 2
  memory_mb = 2048
